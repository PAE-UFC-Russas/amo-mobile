import React from 'react';
import { Button, Center, Text, IconButton } from 'native-base';
import { MaterialIcons } from '@expo/vector-icons'; 

export default function ForumQuest(quest){
    return(
        <Center>
            <Text>
                eae
            </Text>
        </Center>
    )
};